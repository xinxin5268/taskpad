---
name: taskpad
description: "TaskPad — 会话任务临时记忆自动机 + 任务提示板。解决 AI Agent 跨轮次/跨会话记忆断裂问题。"
---

# 🦞 TaskPad — 会话任务临时记忆自动机

> **你的 Agent 再也不会失忆了。**

---

## 先看竞品：哪些事已经被别人干好了

在问你"TaskPad 到底要做什么"之前，先看已有方案能干什么、不能干什么。

### 已有方案覆盖范围

| 需求 | 已有方案 | 覆盖率 |
|------|---------|--------|
| **跨会话长期记忆** | claude-mem (76K★)、MemOS (9K★)、cognitive-memory | ✅ 非常成熟 |
| **任务理解确认** | 无专门工具 | ❌ 空白 |
| **任务分步执行** | long-task-scheduler、agent-orchestrator | ✅ |
| **Context 自适应分片** | 无专门工具 | ❌ 空白 |
| **可视化任务面板** | claude-workflow (4★)、AgentDesk (0★) | ⚠️ 弱覆盖 |
| **任务守护器** | claude-workflow 有状态守卫 | ⚠️ 部分覆盖 |
| **OpenCode / Trae** | 编码/执行 agent，不负责记忆 | ⚠️ 互补 |
| **会话中间断记忆** | 无专门工具 | ❌ 空白 |

### 排除已有 → 提炼 TaskPad 的独特价值

以下是**最核心、不可替代**的三个功能，已有方案做不了：

---

## 🎯 独家核心功能

### 核心一：任务理解确认门禁

**已有方案没做 + claude-mem 不做 + 没有替代品**

Agent 接到任务直接开干，误解题意也没人知道——这是所有 Agent 框架的共性缺陷。TaskPad 加一道**理解确认门禁**：

```
用户："帮我扫描项目安全漏洞"

❌ 普通 Agent 直接干：
   → 开始跑命令……

✅ TaskPad 先确认：
   "我理解你要：1.扫描代码漏洞 2.检测密钥泄露 3.出报告
    范围：/path/to/project
    [✅ 正确继续] [✏️ 修改] [⛔ 取消]"
```

**不可替代原因：** 没有框架做这件事。claude-mem 只管记忆压缩，不管任务理解。long-task-scheduler 只管分片执行，不问理解对不对。

**可开关：** ✅ 不需要就关掉，回归普通 Agent 行为。

---

### 核心二：上下文感知自动分片 + 断点无缝恢复

**claude-mem、long-task-scheduler 都做不到**

Claude Code / OpenCode 的 session 被压缩/中断后，上下文历史全部丢失。claude-mem 虽然能保存记忆，但它保存的是**压缩过的摘要**，不是**精确的步骤进度**。

TaskPad 的做法：

```
Session 1 执行到步骤 5，Context 到 72%
  → 自动保存 checkpoint.json（精确的步骤 5/12，不是摘要）
  → 暂停

Session 2 启动
  → memory-engine.sh list 检测到未完成任务
  → 自动提示："上次到步骤 5/12，要继续吗？"
  → 从步骤 5 精确恢复（不是靠 AI 回忆）
```

**不可替代原因：**
- claude-mem 保存的是**语义记忆**（"用户好像扫描过安全漏洞"），不是**任务状态**（"步骤 4/12，gitleaks 扫描到 3 个密钥"）
- long-task-scheduler 没有 Context 检测，不解决"Context 满了怎么办"
- OpenCode / Trae 被中断后就是白纸一张

**可开关：** ✅ 不需要精确上下文的简单任务可关闭。

---

### 核心三：可视化任务提示板（浮动控制台）

**claude-mem 没有 + claude-code-workflow 没有 + OpenClaw 没有**

所有现有方案对用户都是"黑箱运行"——Agent 在后台闷头干，用户不知道它到哪了。

TaskPad 提供一个**轻量浮动面板**：

```
┌──────────────────────────────────────────────┐
│  🦞 TaskPad · 任务提示板             [✕] 关闭  │
├──────────────────────────────────────────────┤
│                                              │
│  🔵 项目安全扫描  (执行中)                     │
│     ████████░░░░░░░░ 步骤 4/12  ·  33%       │
│     [⏸️暂停] [🛑停止] [✏️修改] [📝备注]       │
│                                              │
│  🟡 代码重构审核  (已暂停)                     │
│     ██████░░░░░░░░░░░░ 步骤 3/8   ·  37%     │
│     [▶️继续] [🛑停止] [✏️修改] [📝备注]       │
│                                              │
│  ───────────────────────────────────────      │
│  ➕ 添加新任务    ⏰ [立即 | 定时 | 循环]       │
│                                              │
│  ⚙️ [开]理解器 [开]分片 [开]记忆 [开]守护器    │
└──────────────────────────────────────────────┘
```

**不可替代原因：**
- claude-mem 的 web viewer (localhost:37777) 只显示**记忆流日志**，不是**任务控制台**
- AgentDesk (0★) 概念类似但无人维护
- claude-workflow 有面板但只面向 Claude Code，不是 OpenClaw 原生

**可开关：** ✅ 不需要 GUI 的用户完全不用启动面板。

---

### 核心四：任务守护器（不同于 claude-workflow 的守卫）

claude-workflow 有 YAML 状态守卫，但它是**定义在配置文件里的检查点**，不是**运行时自动检测**。

TaskPad 守护器的独特性：
| 检测项 | claude-workflow | TaskPad |
|--------|----------------|---------|
| 超时 | 需手动定义 | ✅ 自动对比预估时间 |
| 偏航 | 不检测 | ✅ 检测执行是否偏离 plan.md |
| 死循环 | 不检测 | ✅ 检测连续 3 次重复操作 |
| 自动干预 | ❌ 停在 guard | ✅ 自动暂停 + 告警 + 提示用户 |

**可开关：** ✅

---

### 核心五：所有功能独立可开关

这条看似简单，但没有一个竞品做到。

| 功能 | 开关 | 默认 | 为什么开/关 |
|------|------|------|------------|
| 理解门禁 | ✅ | ON | 新任务需要确认，但老练用户可能想跳过 |
| 调研方案 | ✅ | ON | 复杂任务需要规划，简单任务不需要 |
| 自切分 | ✅ | ON | 长任务需要，一次性命令不需要 |
| 记忆自动机 | ✅ | ON | 需要断点恢复就开，不用就关 |
| 可视化面板 | ✅ | OFF | 需要看进度才开，不要多一个窗口 |
| 守护器 | ✅ | ON | 保底安全网，但有人不想要 |

---

## 一句话说清 TaskPad 是什么

> **TaskPad = 给 Agent 装上"执行过程中不会失忆"的保障层**
>
> 不是长期记忆（claude-mem 管那个），不是执行框架（OpenCode 管那个），
> 是 **任务级执行记忆 + 可视化控制 + 异常守护**。

### 和 claude-mem 的分工

```
claude-mem:   用户偏好的"1+1=3" → 长期语义记忆
TaskPad:      任务到"步骤 4/12 → 步骤 5" → 执行状态记忆

互补关系：claude-mem 管"你以前知道什么"
          TaskPad 管"你现在干到哪了"
```

---

## 技术架构

```
┌────────────┐   ┌────────────┐   ┌──────────────────────┐
│  理解门禁   │ → │  调研方案   │ → │  自切分执行引擎       │
│  (可关)     │   │  (可关)     │   │  (可关)               │
└────────────┘   └────────────┘   └──────────┬───────────┘
                                             │
                    ┌────────────────────────┘
                    ▼
         ┌──────────────────────────────────────┐
         │  临时记忆自动机 (不可替代核心)          │
         │  checkpoint.json → 断点精确恢复        │
         │  workbench/_active.json → 状态索引     │
         └──────────────┬───────────────────────┘
                        │
              ┌─────────┴─────────┐
              ▼                   ▼
    ┌──────────────────┐  ┌──────────────────┐
    │ 可视化提示板      │  │ 守护器            │
    │ (可关, HTTP 19999)│  │ (可关, cron/后台)  │
    └──────────────────┘  └──────────────────┘
```

### 数据流：和现有 OpenClaw 系统的关系

```
用户 → OpenClaw Gateway
         │
         ▼
    主 Agent (我)
         │
         ├─ 认知记忆 (cognitive-memory) → 长期知识
         ├─ 工具调用 (web_fetch/exec/etc) → 实际行动
         └─ TaskPad → 短时任务状态
              ├─ 理解门禁
              ├─ 自切分执行
              ├─ checkpoint 持久化
              ├─ 提示板 (http://localhost:19999)
              └─ 守护器 (cron)
```

---

## 目录结构

```
taskpad/
├── SKILL.md                     ← 本文件
├── README.md                    ← 快速安装
├── scripts/
│   ├── memory-engine.sh         ← 记忆自动机（写/读/列表/清理）
│   ├── context-slicer.sh        ← Context 分片检测
│   ├── guard.sh                 ← 守护器（超时/偏航/死循环）
│   ├── task-agent.py            ← 任务理解 + 分解引擎
│   └── panel-server.py          ← 提示板 HTTP 服务器 (19999)
├── web/
│   ├── panel.html               ← 浮动控制台
│   ├── panel.js                 ← 控制台逻辑
│   └── panel.css                ← 样式
└── templates/
    └── agents-inject.md         ← AGENTS.md 注入模板
```

---

## 定价（基于竞品分析）

| 版本 | 价格 | 包含功能 | 对标 |
|------|------|----------|------|
| 个人免费 | ¥0 | 理解门禁 + 基础分片 | claude-mem 免费 |
| 个人 Pro | ¥19 | 全套 + 面板 + 守护器 | 一杯奶茶多 8 元 |
| 团队版 | ¥99/月 | 多人协作 + 高级守护规则 | 竞品都免费，但没面板 |
| 企业版 | 定制 | 私有部署 + 白标 | |

**定价逻辑：** 竞品都免费开源，但没一家把"理解门禁+面板+守护器"做成即装即用的产品。¥19 是门槛，不是主要收入来源——**生态价值 > 直接收费**。

---

## 版本规划

| 版本 | 功能 | 测试状态 |
|------|------|----------|
| v0.1 ✅ | 记忆自动机 + 检查点 + AGENTS.md 注入 | 未测试 |
| v0.2 ✅ | Context 分片 | 未测试 |
| v0.3 ✅ | 可视化面板 + API | 未测试 |
| v0.4 ✅ | 守护器 | 未测试 |
| v1.0 | 全部跑通 + 产品页 + 发布 | 待完成 |

---

*本文件是产品设计文档，同时也是 OpenClaw 的可安装 Skill。*
